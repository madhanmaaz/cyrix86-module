from setuptools import setup, find_packages

setup(
    name="madhan",
    version="0.0.1",
    author="madhanmaaz",
    description="install custom packages",
    packages=find_packages(),
    py_modules=["madhan"],
    install_requires=[],
)
